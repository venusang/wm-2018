// import fetchMock from 'fetch-mock';
// import thunkMiddleware from 'redux-thunk';
// import configureStore from 'redux-mock-store';
//
// const mockStore = configureStore([thunkMiddleware]);

describe('check locate', () => {
  it('makes the API call', () => {
    // Test PENDING but this is my approach
    // Need to sort out how to get this working
    // const initialState = {};
    // const store = mockStore(initialState);
    //
    // const testData = {};
    // fetchMock.get('*', testData);
    //
    // const actionResult = store.dispatch(locate())
    //   .then(() => {
    //     const actions = store.getActions();
    //     expect(actions.length).toBe(2);
    //     expect(actions[0]).toEqual(requestLocation());
    //     expect(actions[1]).toEqual(receiveLocation(testData));
    //   });
    //
    // fetchMock.restore();
    // return actionResult;
  });
});
